// `timescale 1ns / 1ps

// module top_tb ();

//   reg clk_100M = 0;

//   localparam clock_period = 10;
//   initial begin
//     forever #(clock_period / 2) clk_100M = ~clk_100M;
//   end

// endmodule
